## Aufgaben
### 1.
```sql
select * from waren;
select * from straftaten;
select * from personen;
select * from standorte;
select * from googleSuchanfragen;
select * from krankheiten;
```

### 2.
```sql
select bezeichnung, preis from waren;
select tatbestand, strafmass from straftaten;
select vorname, nachname from personen;
select nachname, monatlichesEinkommen, kontostand from personen;
select bezeichnung from standorte;
select suchbegriff, zeitpunkt from googleSuchanfragen;
select kontostand from personen;
```

### 3.
```sql
select * from personen where haarfarbe = 'schwarz';
select * from personen where augenfarbe != 'blau';
select nachname, vorname, koerpergroesse from personen where koerpergroesse > 1.8;
select nachname, geschlecht from personen where geschlecht = 'm';
select nachname, geschlecht from personen where geschlecht = 'w';
select * from personen where geburtsdatum < '1950-01-01';
select * from personen where nachname like 'M%';
select * from personen where strasse like '%allee';
select * from personen where geburtsdatum like '%-12-24';
```

### 4.
```sql
select nachname, monatlichesEinkommen from personen where vorname = 'Asen';
select nachname, kontostand from personen where vorname = 'Asen';
select nachname, familienstand from personen where vorname = 'Asen';
select * from personen where kontostand > 100000;
select a.suchbegriff, a.zeitpunkt from personen p, googleSuchanfragen a where a.FK_pNr = p.pNr and p.vorname = 'Asen' and p.nachname = 'Daskalov';
```

### 5.
```sql
select * from personen where geschlecht = 'm' and haarfarbe = 'rot' and augenfarbe = 'blau';
select * from personen where geschlecht = 'w' and geburtsdatum < '1950-01-01';
select * from personen where geschlecht = 'm' and haarfarbe = 'blond' and geburtsdatum > '2007-08-19';
select * from personen where hausnummer = 30 or hausnummer = 36;
```

### 6.
```sql
select nachname, vorname, kontostand, monatlichesEinkommen from personen where nachname = 'Lanzilotti' and vorname = 'Giovanni';
select nachname, vorname, kontostand, monatlichesEinkommen from personen where nachname = 'Lanzilotti' and (vorname = 'Giulia' or vorname = 'Sabrina' or vorname = 'Francesca');
select pNr from personen where nachname = 'Lanzilotti' and vorname = 'Giovanni';
select a.suchbegriff, a.zeitpunkt from personen p, googleSuchanfragen a where a.FK_pNr = p.pNr and p.vorname = 'Giovanni' and p.nachname = 'Lanzilotti';
select a.suchbegriff, a.zeitpunkt from personen p, googleSuchanfragen a where a.FK_pNr = p.pNr and p.vorname = 'Giovanni' and p.nachname = 'Lanzilotti' and a.zeitpunkt < '2033-02-01';

select s.tatbestand, s.strafmass from strafregister r, straftaten s, personen p
where p.vorname = 'Giovanni' and p.nachname = 'Lanzilotti'
and r.FK_pNr = p.pNr and s.idStraftat = r.FK_idStraftat;
```

### 7.
```sql
select * from personen order by geburtsdatum asc;
select * from personen where geschlecht = 'm' order by montalichesEinkommen desc;
select * from personen order by kontostand desc;
select * from personen order by nachname asc, vorname asc, geburtsdatum asc;
select * from krankheiten order by bezeichnung asc;
```

### 8.
```sql
select vorname, nachname, kontostand from personen where vorname = 'Carlotta' and nachname = 'Moritz';
select vorname, nachname, familienstand, beruf from personen where vorname = 'Feodor' and nachname like 'Jakow%';
select * from personen where nachname = 'Freund' and geschlecht = 'w';
select * from personen where vorname = 'Jakob' or nachname = 'Jakob';
select vorname, nachname from personen where monatlichesEinkommen = 5240.43 and familienstand = 'verheiratet';
select * from personen where pNr = 10315 or pNr = 10292 or pNr = 10343 or pNr = 10218 or pNr = 10328 order by nachname, vorname;
select * from personen where pNr = 10315 or pNr = 10292 or pNr = 10343 or pNr = 10218 or pNr = 10328 order by monatlichesEinkommen asc;
```

### 9.
#### Primärschlüssel
* ein Attribut einer Entität (z.B. eine Zahl), die jeden Datensatz einzigartig identifiziert

#### Fremdschlüssel
* ein Attribut, das auf den Primärschlüssel einer anderen Entität verweist

### 10.
```sql
select p.pNr, p.vorname, p.nachname, s.suchbegriff from personen p, googleSuchanfragen s where p.vorname = 'Anna' and p.nachname = 'Bader' and p.pNr = s.FK_pNr;
select p.pNr, p.vorname, p.nachname, s.suchbegriff from personen p, googleSuchanfragen s where p.vorname = 'Anna' and p.nachname = 'Bader' and p.pNr = s.FK_pNr and s.zeitpunkt like '2033-01-%';
select p.pNr, p.vorname, p.nachname, s.suchbegriff from personen p, googleSuchanfragen s where p.vorname = 'Anna' and p.nachname = 'Bader' and p.pNr = s.FK_pNr and p.suchbegriff like '%hacken%';
select p.pNr, p.vorname, p.nachname, s.suchbegriff from personen p, googleSuchanfragen s where p.vorname = 'Anna' and p.nachname = 'Bader' and p.pNr = s.FK_pNr and (p.suchbegriff like '%sprengstoff%' or p.suchbegriff like '%explodiert%');
select p.pNr, p.vorname, p.nachname, s.suchbegriff from personen p, googleSuchanfragen s where p.vorname = 'Djako' and p.nachname = 'Daskalov' and p.pNr = s.FK_pNr;
select s.suchbegriff, s.zeitpunkt from personen p, googleSuchanfragen s where p.vorname = 'Djako' and p.nachname = 'Daskalov' and p.pNr = s.FK_pNr;
select p.nachname, p.vorname, p.ortName from personen p, orte o where p.nachname = 'Lanzilotti' and p.FK_idOrt = o.idOrt;
```

### 11.
```sql
select s.suchbegriff, s.zeitpunkt from personen p, googleSuchanfragen s where p.vorname = 'Asen' and p.nachname = 'Daskalov' and p.pNr = s.FK_pNr;
select s.suchbegriff, s.zeitpunkt from personen p, googleSuchanfragen s where p.vorname = 'Asen' and p.nachname = 'Daskalov' and p.pNr = s.FK_pNr order by s.zeitpunkt asc;
select s.suchbegriff, s.zeitpunkt from personen p, googleSuchanfragen s where p.vorname = 'Asen' and p.nachname = 'Daskalov' and p.pNr = s.FK_pNr and (s.suchbegriff like '%schwarzpulver%' or s.suchbegriff like '%sprengstoff%' or s.suchbegriff like '%dünger%');

select w.bezeichnung, w.preis, t.warentyp from waren w, warenTypen t where w.FK_warentyp = t.idWarentyp;
select m.name, t.bezeichnung from medien m, medienTypen t where w.FK_mediumTyp = t.mediumTyp;
select s.bezeichnung, s.strasse, s.hausnummer from standorte s, aufenthalte a where a.FK_idStandort = s.idStandort and a.FK_pNr = 10328;
select s.bezeichnung, s.strasse, s.hausnummer from standorte s, aufenthalte a where a.FK_idStandort = s.idStandort and (a.FK_pNr = 10488 or a.FK_pNr = 10489 or a.FK_pNr = 10490) order by a.FK_pNr desc, a.datum desc;
select k.FK_pNr as pNr, t.bezeichnung from krankenakten k, krankheiten t where k.FK_pNr = 10489 and k.FK_idKrankheit = t.idKrankheit;
select k.FK_pNr as pNr, t.bezeichnung from krankenakten k, krankheiten t where k.FK_pNr between 10700 and 10799 and k.FK_idKrankheit = t.idKrankheit;
```

### 12.
```sql
select p.vorname, p.nachname, s.tatbestand from personen p, straftaten s, strafregister r where r.FK_pNr = p.pNr and r.FK_idStraftat = s.idStraftat; 
```

---

* In einem Film spielen mehrere Schauspieler
* Ein Schauspieler kann in mehreren Filmen spielen
* Mehrere Filme können mehreren Genres zugeordnet werden
* Filme können einem Genre zugeordnet werden
* Ein Regisseur kann mehrere Filme drehen
* Ein Film kann nur einen Regisseur haben

---

#### Übung relationale Modelle

##### a.
Lehrkräfte:
*Lehrkraft_ID*|...
-|-
...

Klasse:
*Klasse_ID*|...
-|-
...

unterrichtet:
*Lehrkraft_ID*|*Klasse_ID*|Wochenstunden
-|-|-
...

##### b.
Mitglied:
*Mitglied_ID*|...
-|-
...

Sportkurs:
*Kurs_ID*|...
-|-
...

belegt:
*Mitglied_ID*|*Kurs_ID*
-|-
...

##### c.
Mitarbeitende:
*Arbeiter_ID*|arbeitet_in_Abteilung_ID
-|-
...

Abteilung:
*Abteilung_ID*|leitet_Arbeiter_ID
-|-
...

##### 2.
Agentur:
*Name*|Ort
-|-
...

Model:
*MID*|Name|Vorname|Geschlecht|Größe|Agentur_Name
-|-|-|-|-|-
...

Model-Shooting-Teilnahme:
*MID*|*SID*|Honorar
-|-|-
...

Shooting:
*SID*|Termin|Ort|macht_FID|für_PID
-|-|-|-|-
...

Fotograf:
*FID*|Name
-|-
...

Produkt:
*PID*|Bezeichnung|Hersteller
-|-|-
...

---

### Übung Normalisierungen

Fehlerart|Tabelle|Beispiel|Warum ist das ein Problem?|Wie beheben? (Normalisierungsschritt)
-|-|-|-|-
Transitive Abhängigkeit|Bestellungen|BestellID 1: KundenID und KundenAdresse werden gespeichert|Information, auf die mithilfe eines Fremdschlüssels zugegriffen werden kann, wird unnötigerweise zusätzlich gespeichert|N3
Fehlerhafter Attributwert|Bestellungen|BestellID 2|KundenAdresse enthält falschen Wert (Kunde mit KundenID 101 hat Adresse "Hauptstraße 12", aber "Hauptstraße 21" wurde gespeichert)|N2
Duplizierter Wert|Kunden|KundenIDs 105 und 106|Derselbe Kunde wurde zweimal eingespeichert, dadurch können Datensätze mit Fremdschlüsseln möglicherweise nicht erfolgreich miteinander verknüpft werden|N1

---

Gast(GastID, Vorname, Name, Geschlecht, Geburtsjahr, Adresse, MasseurID)
Masseur(MasseurID, Vorname, Name)
Mittel(MittelID, Bezeichnung)
massiert(GastID, MasseurID, Datum)
Behandelt(GastID, MittelID)

```sql
select Name, Vorname, Geburtsjahr from Gast;
select * from Mittel where Bezeichnung like "%Öl%";
select G.Name, B.Datum from Gast as G inner join massiert as B on G.GastID = B.GastID;
select M.Name as MasseurName, G.Name as GastName from Gast as G inner join Masseur as M on G.MasseurID = M.MasseurID;
```